syntax = "proto3";

option csharp_namespace = "GoogleApps";

package appId;

service AppDetails
{
	rpc LoadAppData(AppGuid) returns (Reply);
}
message AppGuid
{
	string guid = 1;
}

message Reply
{
	string message = 1;
}